server:
  port: 8085

springdoc:
  swagger-ui:
    path: "/swagger"
    disable-swagger-default-url: true
    use-root-path: true
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tarifficator-db
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: validate
      default_schema: "${app.schema}"
  liquibase:
    default-schema: "${app.schema}"
    change-log: db/changelog/changelog-master.xml


app:
  schema: "profile_service"
#Дополнительные правила для проверки необходимо описать ниже
x-source:
  rules-validate:
    mail:
      - firstName
      - email
    mobile:
      - phoneNumber
    bank:
      - bankId
      - firstName
      - lastName
      - middleName
      - birthDate
      - passportNumber
    gosuslugi:
      - id
      - bankId
      - lastName
      - firstName
      - middleName
      - dateOfBirth
      - passportNumber
      - birthPlace
      - phoneNumber
      - registrationAddress
